# Web Applications

Each subdirectory here is an independent web application.

## Convention

```
apps/web/
├── core/             # Main Guidy web app (Rspack + React + TS)
├── landing/          # Public-facing landing page (Rspack + React + TS)
└── ...
```

Each app should have its own `package.json` and dev/build/start scripts.
