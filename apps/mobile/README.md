# Mobile Applications

Each subdirectory here is an independent Flutter application.

## Convention

```
apps/mobile/
├── app-name-1/       # Flutter project
├── app-name-2/
└── ...
```

Each app is created with `flutter create` and contains its own `pubspec.yaml`.
