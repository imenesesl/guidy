# Server Applications

Each subdirectory here is an independent backend/server application.

## Convention

```
apps/server/
├── api-name-1/       # e.g. Node.js, NestJS, Express, Go, Python server
├── api-name-2/
└── ...
```

Each server should have its own dependency management and Dockerfile.
